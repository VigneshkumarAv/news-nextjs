export default function Loading(){
    return <p>Loading...</p>;
}

//whenever we are waiting for a component to return its content. it the page is sibling or nested page file then loading will be active for that